var isType = require('tcomb/lib/isType');
var getFunctionName = require('tcomb/lib/getFunctionName');

module.exports = function getTypeName(ctor) {
  if (isType(ctor)) {
    return ctor.displayName;
  }
  return getFunctionName(ctor);
};
